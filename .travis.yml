language: node_js
node_js:
  - "8"
cache:
  yarn: true
  directories:
    - node_modules
before_install:
  - npm install --global yarn --cache-min 999999999
  - npm install --global lerna
install:
  - yarn install
  - lerna bootstrap
script:
  - yarn polkadot-dev-build-travis
