language: node_js
node_js:
  - "8"
cache:
  yarn: true
  directories:
    - node_modules
before_install:
  - npm install -g yarn --cache-min 999999999
install:
  - yarn
script:
  - ./node_modules/.bin/polkadot-dev-build-travis build check coveralls publish test
