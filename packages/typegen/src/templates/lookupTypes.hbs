{{> header }}

{{{ imports }}}

declare module '@polkadot/types/lookup' {
  {{#each modules}}
  {{{name}}}: {
    {{#each items}}
    {{> docs}}
    {{{name}}}: {{{type}}} & AugmentedConst<ApiType>;
    {{/each}}
    {{#unless @root.isStrict}}
    /**
      * Generic const
      **/
    [key: string]: Codec;
    {{/unless}}
  };
  {{/each}}
}
{{> footer }}
