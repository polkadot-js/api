{{> header }}

{{{ importsRelative }}}

declare module '@polkadot/types/types/registry' {
  {{{ importsPackage }}}

  export interface InterfaceTypes {
    {{#each items}}
    {{{this}}}: {{{this}}};
    {{/each}}
  } // InterfaceTypes
} // declare module
