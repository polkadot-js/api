{
  "version": "0.11.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel",
    "check": "eslint packages && flow check",
    "clean": "polkadot-dev-clean-build",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.19.10"
  }
}
