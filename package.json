{
  "version": "0.6.8",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel",
    "check": "eslint packages && flow check",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.14.5",
    "lerna": "^2.5.1"
  }
}
