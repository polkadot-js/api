{
  "version": "0.8.18",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "polkadot-dev-build-babel",
    "check": "eslint packages && flow check",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.17.9",
    "lerna": "^2.5.1"
  }
}
