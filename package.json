{
  "name": "@polkadot/api",
  "version": "0.4.1",
  "description": "A JavaScript wrapper for the Polkadot JsonRPC interface",
  "main": "index.js",
  "keywords": [
    "Polkadot",
    "JsonRPC"
  ],
  "author": "Jaco Greeff <jacogr@gmail.com>",
  "license": "ISC",
  "engines": {
    "node": ">=6.4"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/polkadot-js/api.git"
  },
  "bugs": {
    "url": "https://github.com/polkadot-js/api/issues"
  },
  "homepage": "https://github.com/polkadot-js/api#readme",
  "scripts": {
    "build": "rimraf build && babel src --out-dir build --ignore *.spec.js",
    "check": "eslint src && flow check",
    "test": "cross-env NODE_ENV=test babel-istanbul cover _mocha 'src/*.spec.js' 'src/**/*.spec.js'"
  },
  "devDependencies": {
    "@polkadot/dev": "^0.1.7",
    "@polkadot/api-provider": "^0.1.3"
  },
  "dependencies": {
    "@polkadot/api-format": "~0.3.4",
    "@polkadot/jsonrpc": "~0.3.5",
    "@polkadot/util": "~0.5.2",
    "babel-runtime": "^6.26.0"
  }
}
